package org.xbib.datastructures.yaml;

public enum TokenType {
    ITEM,
    KEY,
    VALUE,
    COMMENT,
    VALUE_LINE,
    VALUE_MULTILINE,
    VALUE_TEXT_PIPE,
    VALUE_TEXT_ANGLE,
    PIPE,
    ANGLE
}
